repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: nginx-test
    chart: bitnami/nginx
    # Missing required fields
    invalid_field: true
    malformed: [
